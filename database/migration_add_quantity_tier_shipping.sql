USE iprintrush;

ALTER TABLE shipping_config
  ADD COLUMN IF NOT EXISTS under_100_rate DECIMAL(10,2) NOT NULL DEFAULT 0.00 AFTER default_flat_rate,
  ADD COLUMN IF NOT EXISTS between_100_199_rate DECIMAL(10,2) NOT NULL DEFAULT 0.00 AFTER under_100_rate,
  ADD COLUMN IF NOT EXISTS over_200_rate DECIMAL(10,2) NOT NULL DEFAULT 0.00 AFTER between_100_199_rate;
