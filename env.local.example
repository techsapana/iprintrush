## Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=iprintrush

## Stripe
# Get these from your Stripe dashboard (Developers → API keys)
STRIPE_SECRET_KEY=sk_test_...

# Get this from Stripe CLI "stripe listen" or from your webhook endpoint in the dashboard
STRIPE_WEBHOOK_SECRET=whsec_...

# Used to build success/cancel URLs if the Origin header isn't present
NEXT_PUBLIC_SITE_URL=http://localhost:3000

